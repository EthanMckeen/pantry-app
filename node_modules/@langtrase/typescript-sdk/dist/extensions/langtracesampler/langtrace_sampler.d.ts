import { VendorTracedFunctions } from '@langtrase/trace-attributes/dist/constants/common';
import { SpanKind, Attributes, Context } from '@opentelemetry/api';
import { Sampler, SamplingResult } from '@opentelemetry/sdk-trace-base';
export declare class LangtraceSampler implements Sampler {
    private readonly _disabled_function_names;
    constructor(disabled_functions: Partial<VendorTracedFunctions> | undefined);
    shouldSample(context: Context, traceId: string, spanName: string, spanKind: SpanKind, attributes: Attributes): SamplingResult;
}
