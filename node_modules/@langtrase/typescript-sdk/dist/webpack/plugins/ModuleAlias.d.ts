import { Resolver } from '../../webpack';
export declare class ModuleAlias {
    supportedModuleAliases: string[];
    cwd: string;
    constructor(cwd: string);
    apply(resolver: Resolver): void;
}
