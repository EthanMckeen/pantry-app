import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
declare class QdrantInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(qdrant: any): void;
    init(): Array<InstrumentationModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const qdrantInstrumentation: QdrantInstrumentation;
export {};
