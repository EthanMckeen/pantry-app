import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
declare class LlamaIndexInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(llamaIndex: any): void;
    init(): Array<InstrumentationModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const llamaIndexInstrumentation: LlamaIndexInstrumentation;
export {};
