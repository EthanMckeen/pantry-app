import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
declare class PgInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(pg: any): void;
    init(): Array<InstrumentationModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const pgInstrumentation: PgInstrumentation;
export {};
