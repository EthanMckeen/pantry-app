import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
declare class ChromaInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(chroma: any): void;
    init(): Array<InstrumentationModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const chromaInstrumentation: ChromaInstrumentation;
export {};
