import { InstrumentationBase, InstrumentationNodeModuleDefinition } from '@opentelemetry/instrumentation';
declare class OpenAIInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(openai: any): void;
    init(): Array<InstrumentationNodeModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const openAIInstrumentation: OpenAIInstrumentation;
export {};
