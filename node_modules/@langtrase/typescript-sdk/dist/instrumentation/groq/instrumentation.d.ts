import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
declare class GroqInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(groq: any): void;
    init(): Array<InstrumentationModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const groqInstrumentation: GroqInstrumentation;
export {};
