import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
declare class PineconeInstrumentation extends InstrumentationBase<any> {
    constructor();
    manualPatch(pinecone: any): void;
    init(): Array<InstrumentationModuleDefinition<any>>;
    private _patch;
    private _unpatch;
}
export declare const pineconeInstrumentation: PineconeInstrumentation;
export {};
